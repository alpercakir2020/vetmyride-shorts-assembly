node_modules/
tmp/
.env
.env.local
.DS_Store

# Generated working files (per-run output, never committed)
/*.mp4
/*.wav

# Persona PNG still pending (will commit when generated)
assets/persona/*.png
assets/persona/*.jpg

# Asset libraries (SFX + music + fonts) ARE committed — Pixabay free
# license confirmed for the audio files in this repo.
